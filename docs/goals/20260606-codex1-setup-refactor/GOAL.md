/goal Objective: Refactor Codex1 setup maintenance into a deep Setup bundle module and validation flow so changing, retiring, validating, publishing, and locally fleet-preparing the bundle is direct and agent-drivable, verified by tests, generated/checked artifacts, and updated docs while preserving Setup bundle safety, user-authored files, Mission scaffold artifacts, backups, path safety, and the Anti-Oracle Rule.

Context: Read docs/goals/20260606-codex1-setup-refactor/PREP.md and READINESS_AUDIT.md, then AGENTS.md, CONTEXT.md, README.md, docs/agents/*.md, docs/setup-bundle.md, and docs/cli-contract.md. Current baseline: setup catalog, marker, expected bodies, legacy sets, fingerprints, docs, tests, and updater script are duplicated across src/setup/catalog.rs, tests/common/mod.rs, tests/setup.rs, .codex1/setup-bundle.json, docs, and update-codex1-setups.sh. Start from the dirty worktree without discarding existing setup/skill-removal changes.

Success criteria: One authoritative bundle manifest/interface drives current file inventory, skill/doc categories, marker generation/checking, expected bodies, retired/legacy recognition, and tests; no hand-maintained duplicate current bundle lists remain outside that interface. Adding or retiring one managed Setup bundle file has a documented playbook and requires at most 3 source/artifact edit points excluding the file content itself, with an automated drift check. Install/enable/disable/uninstall/status/doctor/backups behavior and safety are preserved, including refusal to overwrite/remove user-authored files, backups before mutation, legacy managed removal only by managed proof, dry-run no writes, and no Mission scaffold deletion. Fleet updater path is safer and more agent-drivable via local validation/dry-run/fixture coverage and clearer dirty-source behavior; no actual fleet apply, commit, or push is required. Docs explain the new maintenance flow without implying Codex1 owns native goal/workflow state. Required final checks pass: cargo fmt -- --check, cargo test, cargo run --quiet -- --json setup status, cargo run --quiet -- --json setup doctor, cargo run --quiet -- --json setup install --dry-run, and updater syntax/fixture checks.

Feedback loop: Between iterations, run the smallest relevant test or check first, compare failures against PREP.md metrics, choose the next action that increases locality and removes duplication while preserving existing behavior, and update notes before continuing. Prefer TDD for behavior changes and the improve-codebase-architecture lane for module-depth decisions.

Tracking: Keep docs/goals/20260606-codex1-setup-refactor/notes.md, or a linked notes.md in that directory, with running notes, decisions, measurements, blockers, commands, failed attempts, and next steps.

Constraints: Do not discard or overwrite user changes. Do not use destructive git commands. Do not weaken setup overwrite/remove safety, backup/restore safety, path containment, dry-run behavior, or legacy compatibility. Do not turn Codex1 CLI, setup status, doctor, marker, init, docs, or artifacts into a Native goal/workflow-state oracle. Do not run fleet updater apply/commit-push, commit, push, or stage unrelated files unless Joel explicitly asks.

Completion report: Report evidence, commands run and results, changed artifacts, residual risks, cleanup/review performed, final dirty-worktree summary, and whether any actual fleet update was intentionally deferred.
