/goal Objective: Refactor Codex1 setup maintenance into a simpler, net-subtractive Setup bundle module and validation flow so changing, retiring, validating, publishing, and locally fleet-preparing the bundle is direct and agent-drivable, verified by tests, drift checks, a subtraction ledger, and docs while preserving Setup bundle safety, user-authored files, Mission scaffold artifacts, backups, path safety, and the Anti-Oracle Rule.

Context: Read docs/goals/20260606-codex1-setup-refactor/PREP.md and READINESS_AUDIT.md, then AGENTS.md, CONTEXT.md, README.md, docs/agents/*.md, docs/setup-bundle.md, and docs/cli-contract.md. Baseline: setup catalog, marker, expected bodies, legacy sets, fingerprints, docs, tests, and updater script are duplicated across src/setup/catalog.rs, tests/common/mod.rs, tests/setup.rs, .codex1/setup-bundle.json, docs, and update-codex1-setups.sh. Start from the dirty worktree without discarding existing setup/skill-removal changes.

Success criteria: One authoritative bundle interface drives current file inventory, skill/doc categories, marker generation/checking, expected bodies, retired/legacy recognition, and tests; no hand-maintained duplicate current bundle lists remain outside it. The final design is net subtractive: any new manifest, generator, helper, table, or module must replace old machinery before completion, not live beside `catalog.rs` arrays, marker JSON ceremony, duplicate test inventories, or updater shell ceremony as another source of truth. Add a subtraction ledger listing duplicated inventories, arrays, helpers, docs, and manual steps removed or superseded; completion fails if the ledger is net additive in authority, edit points, or workflow. Adding or retiring one managed Setup bundle file has a documented playbook and requires at most 3 source/artifact edit points excluding file content, with an automated drift check. Install/enable/disable/uninstall/status/doctor/backups behavior and safety are preserved, including refusal to overwrite/remove user-authored files, backups before mutation, legacy managed removal only by managed proof, dry-run no writes, and no Mission scaffold deletion. Fleet updater path is simpler and more agent-drivable via local dry-run/fixture coverage and clearer dirty-source behavior; no actual fleet apply, commit, or push is required. Docs explain the new maintenance flow without implying Codex1 owns native goal/workflow state. Required final checks pass: cargo fmt -- --check, cargo test, setup status, setup doctor, setup install --dry-run, and updater syntax/fixture checks.

Feedback loop: Between iterations, run the smallest relevant test/check first, compare failures against PREP.md metrics, choose the next action that removes or supersedes the most duplicated maintenance burden while preserving behavior, and update notes. Prefer TDD for behavior changes and improve-codebase-architecture for module-depth decisions.

Tracking: Keep docs/goals/20260606-codex1-setup-refactor/notes.md, or a linked notes.md in that directory, with running notes, decisions, measurements, blockers, commands, failed attempts, and next steps.

Constraints: Do not discard or overwrite user changes. Do not use destructive git commands. Do not weaken setup overwrite/remove safety, backup/restore safety, path containment, dry-run behavior, or legacy compatibility. Do not add a permanent manifest/generator/helper unless it deletes or supersedes the old maintenance path. Do not turn Codex1 CLI, setup status, doctor, marker, init, docs, or artifacts into a Native goal/workflow-state oracle. Do not run fleet updater apply/commit-push, commit, push, or stage unrelated files unless Joel explicitly asks.

Completion report: Report evidence, commands/results, changed artifacts, subtraction ledger, before/after maintenance edit points, residual risks, cleanup/review, final dirty-worktree summary, and whether any actual fleet update was deferred.
