interface:
  display_name: "Update Codex1 Setups"
  short_description: "Refresh and publish tracked local Codex1 setup repos"
  default_prompt: "Use $update-codex1-setups to dry-run, apply, and optionally commit/push setup-only updates."
