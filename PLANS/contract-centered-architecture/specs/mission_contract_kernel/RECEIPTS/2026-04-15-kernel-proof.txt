Mission: contract-centered-architecture
Spec: mission_contract_kernel
Date: 2026-04-15

Changed files
- crates/codex1-core/src/mission_contract.rs
- crates/codex1-core/src/ralph.rs
- crates/codex1-core/src/lib.rs
- crates/codex1-core/src/runtime.rs

Proof commands
- cargo fmt --all
- cargo test -p codex1-core --quiet
  Result: 77 passed; 0 failed
- cargo test -p codex1 --test runtime_internal --quiet
  Result: 19 passed; 0 failed
- cargo run -q -p codex1 -- internal resolve-resume --repo-root /Users/joel/codex1 --input-json - --json <<EOF {"mission_id":"contract-centered-architecture"} EOF
  Result: governing artifact drift path no longer reports `program_blueprint_fingerprint_drift`; resume now advances to the next honest branch.
- cargo run -q -p codex1 -- internal validate-mission-artifacts --repo-root /Users/joel/codex1 --mission-id contract-centered-architecture --json
  Result: visible artifacts remain clean; machine-state validator still reports a separate cached-state mismatch because it compares state.json only to closeout-derived rebuild state and does not yet account for resume-time gate overrides.

Execution notes
- Introduced MissionContractSnapshot as the native kernel snapshot type.
- Kept RalphState as a compatibility alias so cached state remains projection-stable.
- Moved closeout legality and snapshot derivation into the kernel module.
- Added regression coverage for interrupted-cycle waiting-identity clearing and snapshot projection parity.
- Fixed runtime fingerprint checking so resume compares closeout blueprint fingerprints against the full blueprint contract fingerprint, including execution-graph participation.
