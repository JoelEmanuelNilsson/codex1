reviewer_evidence_snapshot_contract proof receipt
date: 2026-04-16
mission: reviewer-lane-capability-boundary
spec: reviewer_evidence_snapshot_contract
executed_package: 4e8b50f7-0934-4c12-90ce-79ccb8ccb489

implemented changes
- added `ReviewEvidenceSnapshot` and `ReviewEvidenceSnapshotValidationReport`
- added hidden snapshot paths under `.ralph/missions/<mission-id>/review-evidence-snapshots/`
- added `capture-review-evidence-snapshot` and `validate-review-evidence-snapshot`
- evidence snapshots include bundle id, source package id, governing fingerprints, proof rows, receipts, changed-file context, evidence refs, reviewer instructions, and the review truth snapshot
- validation rejects missing proof rows, missing receipts, missing changed-file context, missing evidence refs, missing `NONE` contract, and missing no-mutation reviewer instructions
- validation rejects mismatches between snapshot and source-bundle proof rows, review lenses, receipts, changed-file refs, and touched interface contracts
- review truth snapshots skip `review-evidence-snapshots/` to avoid parent-created frozen briefs self-contaminating the mutation guard
- mission-close evidence snapshots carry and validate mission-level proof rows, cross-spec claims, visible artifact refs, deferred/descoped refs, and open finding summaries
- updated `$review-loop`, internal orchestration docs, runtime backend docs, and Multi-Agent guidance to prefer frozen evidence snapshots for child reviewer briefs

verification
- cargo test -p codex1 --test runtime_internal reviewer_evidence_snapshot_contract --quiet
- cargo check -p codex1
- cargo fmt --all

notes
- The snapshot does not replace reviewer judgment; it provides a frozen bounded brief before live mutable repo reads.
- Parent-owned review truth snapshot remains the mutation guard used for final writeback.
