reviewer_lane_mutation_guard proof receipt
date: 2026-04-16
mission: reviewer-lane-capability-boundary
spec: reviewer_lane_mutation_guard
executed_package: 0d2b6a6e-b0c4-48f3-b765-6895b6d9934c

implemented changes
- added `ReviewTruthSnapshot` to parent-owned review outcome input
- added `capture-review-truth-snapshot` as the deterministic parent pre-review snapshot helper
- guarded `record-review-outcome` so supplied snapshots reject contaminated review waves before gate writeback
- tightened the CLI command so `record-review-outcome` requires `review_truth_snapshot`
- watched both visible `PLANS/<mission-id>/` truth and hidden `.ralph/missions/<mission-id>/` runtime truth
- added regression coverage for contaminated child reviewer mutation and clean parent writeback
- added regression coverage proving snapshot-less review writeback is rejected
- updated review-loop and orchestration docs to require the snapshot guard

verification
- cargo test -p codex1 --test runtime_internal reviewer_lane_mutation_guard --quiet
- cargo test -p codex1 --test runtime_internal internal_runtime_flow_creates_mission_package_and_review_contracts --quiet
- cargo test -p codex1 --test runtime_internal visible_artifact_validation_requires_review_ledger_after_review --quiet
- cargo test -p codex1 --test runtime_internal newer_packages_and_bundles_stale_older_artifacts --quiet
- cargo test -p codex1 --test runtime_internal findings_only_reviewer_stop_hook_bypasses_parent_review_gate_block --quiet
- cargo test -p codex1-core --quiet
- cargo build -p codex1
- cargo fmt --all

notes
- `review_truth_snapshot` is additive for older callers, but the public `$review-loop` workflow now requires it for child-review waves.
- The guard is detection-plus-rejection rather than platform-level sandboxing, matching the native Codex constraints captured in the lock.
