# Spec Notes

- Mission id: `global-codex1-installer`
- Spec id: `global_setup_backup`

Use this file for bounded local notes, spike observations, or drafting scratch
that supports the spec but does not override it.

## Active Notes

- Execution completed for the global setup backup slice on 2026-04-17.
- Receipt: `RECEIPTS/2026-04-17-global-setup-backup.md`.
- The slice intentionally covers user-level `config.toml` and `hooks.json`; global guidance/doctor/restore behavior remains for later governed slices.

## Caution

If a note changes the actual contract, move that change into `SPEC.md` or the
appropriate higher-layer artifact instead of letting this file become hidden
truth.
