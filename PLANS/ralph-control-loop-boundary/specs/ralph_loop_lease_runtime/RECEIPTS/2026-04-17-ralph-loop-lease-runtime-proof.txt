ralph_loop_lease_runtime proof receipt
date: 2026-04-17
mission: ralph-control-loop-boundary
spec: ralph_loop_lease_runtime
package: a31920b3-a02f-4b48-a74a-2807c7bd0cb5

implemented changes
- added a durable Ralph loop lease state at `.ralph/loop-lease.json`
- added internal loop-lease commands: `begin-loop-lease`, `pause-loop-lease`, `clear-loop-lease`, and `inspect-loop-lease`
- changed Stop-hook behavior so generic subagent payloads yield before mission resume handling
- changed parent Stop-hook behavior so actionable mission work blocks only when an active parent loop lease exists
- preserved waiting/needs-user yields and malformed selection-state blocking behavior
- preserved explicit parent-loop enforcement by binding active leases to a mission id and resolving resume for that mission
- documented the runtime command surface and lease-scoped Stop-hook contract in `docs/runtime-backend.md`

verification
- cargo test -p codex1 --test runtime_internal ralph_control_loop_boundary --quiet
- cargo test -p codex1 --test runtime_internal findings_only_reviewer_stop_hook_bypasses_parent_review_gate_block --quiet
- cargo test -p codex1 --test runtime_internal manual_ratified_clarify_yields_for_explicit_plan_instead_of_blocking --quiet
- cargo test -p codex1 --test runtime_internal --quiet
- cargo fmt --all --check
- codex1 internal validate-mission-artifacts --mission-id ralph-control-loop-boundary

notes
- The hook file remains manually paused in this workspace; this slice implements the runtime control boundary but does not restore `.codex/hooks.json`.
- Public skill lease acquisition and first-class close/pause UX are intentionally deferred to `loop_skill_surface_and_pause`.

review repair addendum
- After review found a reproducibility gap in mission artifact validation, the cached mission state was reconciled to the authoritative review-blocked closeout.
- Reproduced validation after repair with both command forms:
  - `target/debug/codex1 internal validate-mission-artifacts --mission-id ralph-control-loop-boundary`
  - `cargo run -q -p codex1 -- internal validate-mission-artifacts --mission-id ralph-control-loop-boundary`

second repair addendum
- Root-caused the recurring validation drift to `resolve_resume` persisting synthetic `open_review_gate` resume prompts into `state.json`.
- Updated runtime resume handling so open-review-gate prompts are report-only and cached state remains the closeout-derived authoritative state.
- Rebuilt `target/debug/codex1`, reran `resolve-resume`, and reproduced both validation command forms successfully after the runtime fix:
  - `target/debug/codex1 internal validate-mission-artifacts --mission-id ralph-control-loop-boundary`
  - `cargo run -q -p codex1 -- internal validate-mission-artifacts --mission-id ralph-control-loop-boundary`
